import operator
d = {1: 2, 3: 4, 4: 3, 2: 1, 0: 0}
asc = print("display by ascently: ", sorted(d.items(),key=lambda x:x[1])) #按字典的value从小到大排序
original_d = print(d)
desc = print("display by desc: ", sorted(d.items(),key=lambda x:x[1], reverse=True)) # 按字典的value从大到小排序
